{
  "name": "steamboq-proposal-estimator",
  "private": true,
  "version": "1.0.0",
  "type": "module",
  "scripts": {
    "dev": "vite --host 0.0.0.0",
    "build": "node scripts/build.mjs",
    "preview": "vite preview --host 0.0.0.0",
    "test": "npm run build && node --test",
    "test:engine": "node --test test/engine.test.js"
  },
  "devDependencies": {
    "vite": "^7.1.3"
  }
}
